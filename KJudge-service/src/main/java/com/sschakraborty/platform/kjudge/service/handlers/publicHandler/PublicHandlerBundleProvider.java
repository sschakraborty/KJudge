package com.sschakraborty.platform.kjudge.service.handlers.publicHandler;

import com.sschakraborty.platform.kjudge.service.handlers.HandlerBundleProvider;
import io.vertx.ext.web.Router;

public class PublicHandlerBundleProvider implements HandlerBundleProvider {
	@Override
	public void applyRoutes(Router router) {

	}
}